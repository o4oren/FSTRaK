<UserControl x:Class="FSTRaK.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSTRaK.Views"
             mc:Ignorable="d" 
             xmlns:viewmodels="clr-namespace:FSTRaK.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="OnLoaded"
             >
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label>Map tiles Provider</Label>
        <ComboBox Grid.Column="1" ItemsSource="{Binding MapProviders}" SelectedItem="{Binding SelectedMapProvider}"/>
        <Label Grid.Row="1">Bing API Key</Label>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding BingApiKey}" TextWrapping="WrapWithOverflow" Height="50" Width="200"/>
    </Grid>
</UserControl>

<UserControl x:Class="FSTRaK.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FSTRaK.Views" xmlns:viewmodels="clr-namespace:FSTRaK.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:StatisticsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal" Margin="10" >
                <Label  Style="{StaticResource FSTrAkLabel}" Width="250">Total Flights:</Label>
                <Label  Style="{StaticResource FSTrAkLabel}" Width="250" Content="{Binding TotalNumberOfFlights}"/>

            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>

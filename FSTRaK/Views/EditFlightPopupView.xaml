<UserControl x:Class="FSTRaK.Views.EditFlightPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FSTRaK.ViewModels"
             mc:Ignorable="d"
             Style="{DynamicResource GlobalFont}"
             d:DataContext="{d:DesignInstance Type=viewModels:EditFlightViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Popup
        Name="EditFlightPopup"
        IsOpen="{Binding IsShow}"
        PlacementTarget="{Binding ElementName=EditFlightPopup}"
        Placement="MousePoint"
    >
        <Border
            BorderBrush="{DynamicResource BorderMidColorBrush}"
            Background="{DynamicResource PrimaryLightBrush}"
            BorderThickness="1"
            Padding="10"
        >
            <Grid>
                <Grid.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="FontFamily" Value="{DynamicResource CurrentFont}"/>
                        <Setter Property="FontSize" Value="{DynamicResource ListFontSize}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                    <Style TargetType="{x:Type ComboBox}">
                        <Setter Property="FontFamily" Value="{DynamicResource CurrentFont}"/>
                        <Setter Property="FontSize" Value="{DynamicResource ListFontSize}"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="IsEditable" Value="True"/>
                        <Setter Property="IsTextSearchEnabled" Value="False"/>
                        <Setter Property="TextSearch.TextPath" Value="Ident"/>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0">Departure</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="0"
                    ItemsSource="{Binding NearbyDepartureAirports}"
                    Text="{Binding DepartureAirport, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}" Margin="0"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Grid.Column="0" Grid.Row="1">Arrival</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="1"
                    ItemsSource="{Binding NearbyArrivalAirports}"
                    Text="{Binding ArrivalAirport, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}" Margin="0"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"
                    Foreground="Red"
                    TextWrapping="Wrap"
                    Text="{Binding ValidationMessage}"/>

                <StackPanel Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2"
                    Orientation="Horizontal" Margin="5,8,5,3">
                    <Button Command="{Binding UpdateFlight}" Width="100" Margin="0,0,8,0">Save</Button>
                    <Button Command="{Binding ClosePopup}" Width="100">Close</Button>
                </StackPanel>
            </Grid>
        </Border>
    </Popup>
</UserControl>

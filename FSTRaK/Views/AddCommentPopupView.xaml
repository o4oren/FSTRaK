<UserControl x:Class="FSTRaK.Views.AddCommentPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FSTRaK.ViewModels"
             mc:Ignorable="d"
             Style="{DynamicResource GlobalFont}"
             d:DataContext="{d:DesignInstance Type=viewModels:AddCommentViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Popup
        Name="AddCommentPopup"
        IsOpen="{Binding IsShow}"
        Placement="MousePoint"
    >
        <Border
            BorderBrush="{DynamicResource BorderMidColorBrush}"
            Background="{DynamicResource PrimaryLightBrush}"
            BorderThickness="1"
            Padding="10"
        >
            <StackPanel Width="320">
                <TextBlock
                    FontFamily="{DynamicResource CurrentFont}"
                    FontSize="{DynamicResource ListFontSize}"
                    Foreground="{DynamicResource TextColor}"
                    Margin="5,5,5,3">Comment</TextBlock>
                <TextBox
                    Style="{DynamicResource FSTrAkTextBox}"
                    Margin="5,0,5,8"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    MinLines="5"
                    MaxLines="10"
                    MaxLength="180"
                    VerticalScrollBarVisibility="Auto"
                    Text="{Binding Flight.Comment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="5,0,5,5">
                    <Button Command="{Binding AddComment}" Width="100" Margin="0,0,8,0">Save</Button>
                    <Button Command="{Binding ClosePopup}" Width="100">Close</Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Popup>
</UserControl>
